// import { UserData } from '@src/types/users'
export const setToken = (token: string) => ({
    type: 'SET_TOKEN', 
    token,
  });

  // export const userLogout = () => ({
  //   type: 'USER_LOGOUT',
  // });