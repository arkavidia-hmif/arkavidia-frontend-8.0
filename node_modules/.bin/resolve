#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../eslint-plugin-n/node_modules/resolve/bin/resolve" "$@"
  ret=$?
else 
  node  "$basedir/../eslint-plugin-n/node_modules/resolve/bin/resolve" "$@"
  ret=$?
fi
exit $ret
